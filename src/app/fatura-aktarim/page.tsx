'use client';

import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/card';

export default function FaturaAktarimPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Fatura Aktarımı</CardTitle>
        <CardDescription>Bu modül yakında burada olacak.</CardDescription>
      </CardHeader>
      <CardContent>
        {/* Placeholder content */}
      </CardContent>
    </Card>
  );
}
