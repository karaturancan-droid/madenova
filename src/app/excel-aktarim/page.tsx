'use client';

import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/card';

export default function ExcelAktarimPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Excel Aktarımı</CardTitle>
        <CardDescription>Bu modül yakında burada olacak.</CardDescription>
      </CardHeader>
      <CardContent>
        {/* Placeholder content */}
      </CardContent>
    </Card>
  );
}
